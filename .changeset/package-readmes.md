---
'@svelte-pdf/engine': patch
'@svelte-pdf/renderer': patch
'@svelte-pdf/markdown': patch
---

Add READMEs to the engine and renderer packages so npm pages render documentation, and fix the repository link in the markdown package README.
