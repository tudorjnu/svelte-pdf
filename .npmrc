# Publishing auth for changeset publish / npm publish. The token itself
# never lives in this file: npm expands ${NPM_TOKEN} from the environment
# (locally via .env, which bun loads automatically; in CI the
# changesets/action injects the same variable from the repo secret).
//registry.npmjs.org/:_authToken=${NPM_TOKEN}